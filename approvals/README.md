# Immutable approval records

This directory is intentionally empty of approval records.

Future records use `approvals/<releaseId>.json`. An approval is immutable and must bind the exact release identifier to the SHA-256 of its manifest. Existing approvals must never be changed or deleted.
